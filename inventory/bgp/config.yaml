inventory:
    plugin: SimpleInventory
    options:
        host_file: "inventory/bgp/hosts.yaml"
        group_file: "inventory/bgp/groups.yaml"
        defaults_file: "inventory/bgp/defaults.yaml"
runner:
    plugin: threaded
    options:
        num_workers: 100